{
  "name": "claude-pr-review-task",
  "version": "1.0.0",
  "description": "Azure DevOps task for PR review using Claude Code CLI",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "package": "tsc && npm prune --production"
  },
  "dependencies": {
    "azure-pipelines-task-lib": "^4.17.0",
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0"
  }
}
